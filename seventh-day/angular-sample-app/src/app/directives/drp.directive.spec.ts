import { DrpDirective } from './drp.directive';

describe('DrpDirective', () => {
  it('should create an instance', () => {
    const directive = new DrpDirective();
    expect(directive).toBeTruthy();
  });
});
