import { CtmStrDirective } from './ctm-str.directive';

describe('CtmStrDirective', () => {
  it('should create an instance', () => {
    const directive = new CtmStrDirective();
    expect(directive).toBeTruthy();
  });
});
